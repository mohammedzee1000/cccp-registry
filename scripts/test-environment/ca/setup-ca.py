#!/bin/python

# imports
import ConfigParser

# Base variables
CA_LOC = "/root"

# Consts
CA_CERT = "ca.cert.pem"
CA_KEY = "ca.key.pem"

# Derived
CA_DIR = CA_LOC + "/ca"
INT_DIR = CA_DIR + "/intermediate"

